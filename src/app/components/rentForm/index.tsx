import { FormBox, FromWrapper } from "@/styles/form";
import { Box } from "@mui/material";
import React from "react";

type Props = {};

function Form({}: Props) {
  return (
    <FromWrapper>
      <FormBox>
        <Box></Box>
      </FormBox>
    </FromWrapper>
  );
}
export default Form;
